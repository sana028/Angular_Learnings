import { IfdirectiveDirective } from './ifdirective.directive';

describe('IfdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
